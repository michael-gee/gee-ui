import { Canvas, Meta, Story } from '@storybook/blocks';
import * as AlertDialogStories from './AlertDialog.stories';
import { RadixDocs } from '../_internal/RadixDocs';

<Meta of={AlertDialogStories} />

# Alert Dialog

<p className="italic">A modal dialog that interrupts the user with important content and expects a response.</p>

<RadixDocs componentName="alert-dialog" />

### Default

<Canvas of={AlertDialogStories.Default} />

### Dark Mode

<div className="story-dark">
  <Story of={AlertDialogStories.DarkMode} />
</div>
