import { Canvas, Meta, Story } from '@storybook/blocks';
import * as DialogStories from './Dialog.stories';
import { RadixDocs } from '../_internal/RadixDocs';

<Meta of={DialogStories} />

# Dialog

<p className="italic">
  A window overlaid on either the primary window or another dialog window, rendering the content underneath inert.
</p>

<RadixDocs componentName="dialog" />

### Default

<Canvas of={DialogStories.Default} />

### Dark Mode

<div className="story-dark">
  <Story of={DialogStories.DarkMode} />
</div>

### With Custom Close Button

<Canvas of={DialogStories.WithCustomClose} />
