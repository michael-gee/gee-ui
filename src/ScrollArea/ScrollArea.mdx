import { Canvas, Meta } from '@storybook/blocks';
import * as ScrollAreaStories from './ScrollArea.stories';
import { RadixDocs } from '../_internal/RadixDocs';

<Meta of={ScrollAreaStories} />

# ScrollArea

<p className="italic">Augments native scroll functionality for custom, cross-browser styling.</p>

<RadixDocs componentName="scroll-area" />

### Default

<Canvas of={ScrollAreaStories.Default} />

### Dark Mode

<Canvas of={ScrollAreaStories.DarkMode} className="canvas-dark" />
