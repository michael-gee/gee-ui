import { Canvas, Meta } from '@storybook/blocks';
import * as CollapsibleStories from './Collapsible.stories';

<Meta of={CollapsibleStories} />

# Collapsible

An interactive component which expands/collapses a panel.

### Default

<Canvas of={CollapsibleStories.Default} />

### Dark Mode

<Canvas of={CollapsibleStories.DarkMode} className="canvas-dark" />

### Simple (Unstyled)

<Canvas of={CollapsibleStories.Simple} />
