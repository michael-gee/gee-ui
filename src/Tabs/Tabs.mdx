import { Canvas, Meta } from '@storybook/blocks';
import * as TabsStories from './Tabs.stories';
import { RadixDocs } from '../_internal/RadixDocs';

<Meta of={TabsStories} />

# Tabs

<p className="italic">A set of layered sections of content—known as tab panels—that are displayed one at a time.</p>

<RadixDocs componentName="tabs" />

### Default

<Canvas of={TabsStories.Default} />

### Dark Mode

<Canvas of={TabsStories.DarkMode} className="canvas-dark" />
