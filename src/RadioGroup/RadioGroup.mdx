import { Canvas, Meta } from '@storybook/blocks';
import * as RadioGroupStories from './RadioGroup.stories';
import { RadixDocs } from '../_internal/RadixDocs';

<Meta of={RadioGroupStories} />

# Radio Group

<p className="italic">
  A set of checkable buttons—known as radio buttons—where no more than one of the buttons can be checked at a time.
</p>

<RadixDocs componentName="radio-group" />

### Default

<Canvas of={RadioGroupStories.Default} />

### Dark Mode

<Canvas of={RadioGroupStories.DarkMode} className="canvas-dark" />
