import { Canvas, Meta, Story } from '@storybook/blocks';
import * as SelectStories from './Select.stories';
import { RadixDocs } from '../_internal/RadixDocs';

<Meta of={SelectStories} />

# Select

<p className="italic">Displays a list of options for the user to pick from—triggered by a button.</p>

<RadixDocs componentName="select" />

### Default

<Canvas of={SelectStories.Default} />

### Dark Mode

<div className="story-dark">
  <Story of={SelectStories.DarkMode} />
</div>

### Scrollable

<Canvas of={SelectStories.Scrollable} />
