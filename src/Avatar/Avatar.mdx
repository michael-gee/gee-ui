import { Canvas, Meta, Story } from '@storybook/blocks';
import * as AvatarStories from './Avatar.stories';
import { DarkModeContainer } from '../_internal/storybook-utils';

<Meta of={AvatarStories} />

# Avatar

An image element with a fallback for representing the user.

<Canvas of={AvatarStories.Default} />

### Dark Mode

<DarkModeContainer className="mx-auto flex w-full justify-center">
  <Story of={AvatarStories.DarkMode} />
</DarkModeContainer>

### Fallback

<Canvas of={AvatarStories.Fallback} />

### Fallback (Dark)

<DarkModeContainer className="mx-auto flex w-full justify-center">
  <Story of={AvatarStories.DarkModeFallback} />
</DarkModeContainer>
