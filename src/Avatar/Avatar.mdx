import { Canvas, Meta } from '@storybook/blocks';
import * as AvatarStories from './Avatar.stories';
import { RadixDocs } from '../_internal/RadixDocs';

<Meta of={AvatarStories} />

# Avatar

<p className="italic">An image element with a fallback for representing the user.</p>

<RadixDocs componentName="avatar" />

<Canvas of={AvatarStories.Default} />

### Dark Mode

<Canvas of={AvatarStories.DarkMode} className="canvas-dark" />

### Fallback

<Canvas of={AvatarStories.Fallback} />

### Fallback (Dark)

<Canvas of={AvatarStories.DarkModeFallback} className="canvas-dark" />
