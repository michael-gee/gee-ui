import { Canvas, Meta } from '@storybook/blocks';
import * as SwitchStories from './Switch.stories';
import { RadixDocs } from '../_internal/RadixDocs';

<Meta of={SwitchStories} />

# Switch

<p className="italic">A control that allows the user to toggle between checked and not checked.</p>

<RadixDocs componentName="switch" />

### Default

<Canvas of={SwitchStories.Default} />

### Dark Mode

<Canvas of={SwitchStories.DarkMode} className="canvas-dark" />
